# 附图清单与引用一致性说明

## 1. 附图文件清单

1. `fig1_system_architecture.png`：系统总体架构图  
2. `fig2_method_flow.png`：方法流程图（S1~S8）  
3. `fig3_disagreement_graph.png`：分歧图谱示意图  
4. `fig4_decoupling_subquestions.png`：结构化解耦与子问题树  
5. `fig5_iteration_feedback_loop.png`：迭代收敛反馈环  
6. `fig6_data_structure.png`：输出结构化结果的数据结构示意

## 2. 摘要附图选择

- 选用：`fig2_method_flow.png`  
- 理由：最直接体现本发明的处理闭环（双模型生成、分歧识别、子问题裁决、融合输出、收敛控制）。

## 3. 附图标记与说明书引用映射

- `101` 获取与标准化模块  
- `102A` 模型A推理模块  
- `102B` 模型B推理模块  
- `103` 解析切分模块  
- `104` 对齐匹配模块  
- `105` 分歧识别模块  
- `106` 结构化解耦模块  
- `107` 证据与裁决模块  
- `108` 融合输出模块  
- `109` 存储审计与合规模块  
- `110` 预算调度与迭代控制模块

## 4. 一致性检查规则

- 说明书中出现的附图编号必须在本清单中存在。  
- 说明书中出现的模块标记（101~110）必须在“附图标记说明”中有定义。  
- 摘要附图必须从本清单中选择，不新增孤立图号。

